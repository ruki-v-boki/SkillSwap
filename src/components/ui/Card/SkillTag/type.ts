import type { Category } from "@/types/types";

// ---------------------------------------------------------------

export type TSkillTagUIProps = {
  name: string;
  category: Category;
  count?: number;
};